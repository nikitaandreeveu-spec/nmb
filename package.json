{
  "name": "nmb",
  "version": "1.0.0",
  "description": "Anthropic PBC strategic landing page",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
